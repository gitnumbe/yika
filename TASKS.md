# TASKS.md — yika v3.0 开发任务清单（P0-P8 全量）

> 开发流程按 AGENTS.md「开发工程师工作原则」：按 TASKS.md 顺序找第一个未完成的 `[ ]`，做完改 `[x]`，跑测试守门。
> 文档依据：`D:\文档类\obsidian\yika\开发文档.md`（v3.0）+ `首期开发切片.md`。
> **铁律**：一次一批不跨批 · 测试不绿不算完 · 不重构不优化 · 文档即真相。

## ⏳ 待适配待办（P0 数据模型重建导致的 v2 业务路由测试，P3 统一定格）

- [ ] **P3 适配**：v2→v3 字段变更影响的 10 个业务路由测试（Note `scene`→`scenario`、Requirement `source_ref` 改用 `source_note_id`、Recording `scene`、`/projects` `/requirements` `/notes` 路由字段），P3 业务路由批次统一改：
  - `test_business_flows.py`（test_requirement_full_lifecycle / adjust_and_reopen / role_permission_boundary）
  - `test_customers_projects.py`（test_create_customer_and_project）
  - `test_pipeline.py`（test_pipeline_orders_stages）
  - `test_recording_flow.py`（test_recording_to_requirement / extract_no_persist）
  - `test_requirements.py`（full_review / illegal_transition / instructor_cannot_transition）
  - `test_tts_endpoint.py`（test_note_tts_synthesize_then_cached）

## 🚦 上报机制（卡住/歧义不要死磕）

- **连续 3 次测试不过** → 在该任务后加 `[⚠️卡住]`，**继续下一个不依赖它的任务**，不要死磕浪费 token。
- **文档找不到答案/有歧义** → 不要猜。在该任务后加 `[❓待澄清]`，**继续下一个不依赖它的任务**。
- 卡住/待澄清会在后续人工介入时集中处理。

---

## 📌 跨批次依赖总览

```
P0(地基) → P1(外壳后端) → P2(外壳前端) → P2.5(测试子系统验证S1-S8)
              ↓              ↓
              P3(主数据) → P4(权限) ─→ P7a(手动流前端,并行)
                  ↓              ↑
              P5(AI:A1/A3/A4) ──→ P6(语音+ASR→A6→A3素材)
                  ↓              ↓
              P2.5通过才允许P3起填业务 → P7b(AI/语音前端) → P8 收尾
```

---

## ■ 批次 P0 · 脚手架与数据层地基（当前 · 顺序执行）

> ⚠️ 仓库现为 v2 遗留，P0 彻底重写数据层为 v3.0。

- [x] **P0.1** 建平台代码骨架：`backend/` 分模块 `platform/` + `subsys/`，保留现有成熟 auth/审计骨架
  - 验收：目录结构清晰，`app/main.py` 能 import，既有骨架不报错
  - **✅ 完成**：core（auth/db/ai 归位）/platform（routers+services）/subsys/collab（routers+services）就位；core 包全量 import 通过；基线 47 测试全绿。见 `backend/app/ARCHITECTURE.md`
- [x] **P0.2a** 组织模型（`Group`/`User`(Role=admin/leader/instructor/developer, User→Group 1:N)/`RefreshToken`）
  - 验收：三表字段含枚举+1:N 关联；建表 SQL 正确；L1 组织模型测试绿
  - **✅ 完成**：Role 改 v3 四角色（tech→developer）；新增 Group；User 加 group_ids(1:N)/display_name/active；登录安全字段保留。测试 `test_org_models.py` 6 例绿；全量 53 测试绿（v2 tech→developer 角色适配完成）。
- [x] **P0.2b** 业务主数据（`Customer`/`Project`/`Requirement`(状态机+source_ref)/`RequirementCandidate`/`Note`）
  - 验收：全表带 `group_id` 冗余；Requirement 含状态机字段+候选需求；L1 主数据测试绿
  - **✅ 完成**：Customer/Project/Requirement/Note 升级 v3（全带 group_id）；新增 RequirementCandidate；状态机/来源/优先级枚举改 v3。测试 `test_business_models.py` 8 例绿；全量 51 过、10 个 v2 业务路由测试因字段变更待 P3 适配（已记录待办）。
- [ ] **P0.2c** 知识+审计+子系统注册（`Knowledge`/`AuditLog`/`Subsystem`）
  - 验收：Knowledge 无组隔离字段（全平台共通）；AuditLog 只增不改；Subsystem 含状态字段；L1 测试绿
  - **✅ 完成**：Knowledge 全平台共通（无 group_id，group_scope=global）；AuditLog append-only（actor/entity/entity_id/ip）；新增 Subsystem（key/roles/status active/stopped/archived）。测试 `test_platform_models.py` 5 例绿；数据模型层 P0.2 a/b/c 共 19 例全绿。
- [ ] **P0.3** 迁移与种子：角色枚举 + 全局管理员 + 示例业务组/组长/讲师/开发
  - 验收：启动后种子数据可查；角色枚举正确
- [ ] **P0.4** 数据库接入 **Alembic**（开发 SQLite / 生产 PostgreSQL 预留）
  - 验收：`alembic upgrade head` 可建表；生成首个 v3.0 迁移脚本
- [ ] **P0.5** 统一错误处理 + 日志骨架
  - 验收：错误响应统一结构；有日志埋点框架

**P0 总验收**：应用能起，空表结构正确，L1 数据模型测试全绿。

---

## ■ 批次 P1 · 平台外壳（后端）

- [x] **P1.1** 统一登录（共享 Cookie 域 JWT/Session，为 iframe 子系统共享登录态打底）
  - 验收：登录发双令牌；Cookie 域配置可切换；失效返回 401
- [x] **P1.2** 组织模型 API（Group/User 管理、组长指派，全局管理员）
  - 验收：管理员能建组/派组长；非管理员 403；L2 组织权限测试绿
- [x] **P1.3** 子系统注册清单（S1：标识/入口/图标/可用角色/状态——增/停/下线标记，粒度2）
  - 验收：注册清单驱动"哪些角色见哪些子系统"；停/下线隐藏入口但数据保留
- [x] **P1.4a** 权限层·外壳导航级 + API 级数据归属过滤（基于 group_id 的中间件）
  - 验收：API 查询自动带 group_id 过滤，跨组读不到；中间件生效
- [x] **P1.4b** 权限层·角色校验依赖 + 界面级权限提示（require_role + 权限查询接口）
  - 验收：`require_role` 各角色生效；前端可查"当前用户能不能做某操作"
- [x] **P1.5** 共享主数据 API（S3：客户/知识/组织统一读写，组隔离在 API 层强制）
  - 验收：共享 API 带组隔离；子系统经此读写不直连库
- [x] **P1.6** 事件总线（S6：RabbitMQ + 事件 schema + at-least-once + 幂等 key + topic 路由）
  - 验收：发事件能被订阅端幂等消费（重复投递只生效一次）

**P1 总验收**：注册清单驱动角色见子系统；共享 API 带组隔离；事件幂等消费。

---

## ■ 批次 P2 · 平台外壳（前端）

- [x] **P2.1** 设计 token 系统（S7：颜色/字体/间距/圆角 CSS 变量，来自视觉方案-版A）
  - 验收：token 走 CSS 变量；页面无硬编码颜色
- [x] **P2.2** 登录页（共享 Cookie 登录，Vue3 + Element Plus）
  - 验收：登录进门户；build 绿；element-plus 组件优先
- [x] **P2.3** 门户工作台页（子系统图标列表，按角色过滤显示）
  - 验收：按角色过滤子系统图标；无角色显示为空/提示
- [x] **P2.4** 子系统外壳容器（iframe 接入点 + 统一导航栏）
  - 验收：iframe 加载子系统；共享登录态打通（同域 Cookie）；跨域坑已处理
- [x] **P2.5** 管理后台（建组/派组长/子系统注册增停）
  - 验收：管理员能操作；非管理员 403；无硬编码权限

**P2 总验收**：登录进门户 → 看可用子系统图标 → 点开进外壳 + iframe 占位。build + E2E 登录通过。

---

## ■ 批次 P2.5 · 测试子系统接入验证（规范可复用性闸门）

- [ ] **P2.5.1** 最小测试子系统（独立 Vue3 + 一行后端，展示 S1 注册→S2 iframe→S3 调共享API→S4 无权限代码→S7 用平台token→S8 可下线）
  - 验收：子系统注册即显示；iframe 加载成功；调共享 API 成功
- [ ] **P2.5.2** 验证 S6 事件（发/收一个广播事件）+ S5 数据归属（读共享客户受组隔离）
  - 验收：事件发收成功；跨组读共享客户被过滤
- [ ] **P2.5.3** 验证 S8 生命周期（注册→显示→停→隐藏保数据→下线标记）
  - 验收：停后入口隐藏且数据保留；下线标记正确
- [ ] **P2.5.4** 记录验证结论回写《子系统规范》；若 S 条款不成立先修规范再进 P3
  - 验收：规范缺口已回写；P2.5 全通过才允许 P3

**P2.5 总验收**：S1-S8 全通、规范可复用性证实。

---

## ■ 批次 P3 · 协作子系统 · 主数据（业务块 1）

- [x] **P3.1** 客户 CRUD（组私有）+ 客户池导入（组长/管理）
  - 验收：组内客户 CRUD；跨组读不到；导入带组隔离
- [x] **P3.2** 项目 CRUD（客户下，一个客户多项目）
  - 验收：项目挂客户下；跨组读不到；L2 测试绿
- [x] **P3.3** 需求 CRUD + 状态机（草稿→待评审→可行/需调整/不可行→开发中→已交付；挂项目下；组长评审）
  - 验收：状态机非法跳转返回400；草稿→待评审→可行→开发中→已交付正向通；三种回退（信息待补/方案待调/不可行）都能走；**组长以外角色评审返回403**
- [x] **P3.4** 需求"来源"溯源（手动录入 OR 候选需求确认，带 source_ref）
  - 验收：需求带 source_ref；候选需求确认后转正式需求；防幻觉（不自动落库）
- [x] **P3.5** 知识库 CRUD（全平台共通）
  - 验收：全平台可查；无组隔离；开发/组长可写、讲师需审核

**P3 总验收**：讲师/开发能建客户→项目→需求走状态机；组长能评审。L2 业务测试绿。

---

## ■ 批次 P4 · 权限矩阵落地

- [x] **P4.1** 组数据隔离强制（客户/项目仅本组可见，知识共通）——API 层
  - 验收：跨组读客户/项目/需求返回空/404；知识全平台可见
- [x] **P4.2** 组长评审权（拍板可行/不可行/需调整）；讲师/开发只能给意见
  - 验收：组长可评审；讲师/开发评审返回403
- [x] **P4.3** 界面级权限提示（前端按 API 返回显示/隐藏评审按钮等）
  - 验收：前端按权限接口渲染按钮，无硬编码权限
- [x] **P4.4** 权限越权测试（**全矩阵**：讲师不能评审、开发不能评审、跨组读不到、越权写）
  - 验收：权限矩阵每格有测试；越权请求403/404；不止测"讲师不能评审"还含跨组

**P4 总验收**：权限矩阵每格有测试覆盖；越权请求 403。L2 安全测试绿。

---

## ■ 批次 P5 · AI 环节（仅 A1/A3/A4）

> 实现前先做技术预研 P5.0 验证链路通。

- [x] **P5.0** 技术预研：跑通 LLM API + 一个最简单 JSON 结构化提取测试（验证链路通）
  - 验收：能真实调通 LLM 并拿到合规 JSON；确认模型/端点可用
- [x] **P5.1a** A1 mock 数据准备（模拟官网/企查查返回）
  - 验收：有 A1 测试用 mock 数据与用例
- [x] **P5.1b** A1 客户画像采集实现 + 准确率测试（关键字段命中≥85%）
  - 验收：自动校验+存疑字段推人工；命中率≥85% 测量脚本
- [x] **P5.2a** A3 mock 数据准备（示例沟通记录/转写文本）
  - 验收：有 A3 测试用示例沟通记录与候选需求用例
- [x] **P5.2b** A3 需求提炼实现 + 准确率测试（候选需求有效≥70%）
  - 验收：候选箱批量编辑确认后落需求；有效≥70%
- [x] **P5.3a** A4 mock 数据准备（示例知识库条目/pgvector 索引）
  - 验收：有 A4 测试用知识库条目与问题用例；pgvector 就绪
- [x] **P5.3b** A4 RAG 答疑实现 + 准确率测试（引用≥90%）
  - 验收：三重保险（未命中/低置信转人+带引用）；引用有效≥90%

**P5 总验收**：每个 AI 环节有 mock 测试 + 准确率底线测量脚本；达标才算完成。

---

## ■ 批次 P6 · 语音链路（录音→ASR→A6→A3 素材接通）

> 实现前先做技术预研 P6.0 验证环境通。

- [x] **P6.0** 技术预研：跑通 ASR 服务 + 一个最短音频的转写测试（验证环境通）
  - 验收：能真实调通 ASR 并拿到转写文本；ffmpeg 16kHz 转码就绪
- [x] **P6.1** 录音上传（MediaRecorder webm → 存录音）
  - 验收：录音上传成功存储；ffmpeg 转 16kHz 单声道 WAV
- [x] **P6.2** ASR 转写（基础设施：Qwen3-ASR 为主 + **provider 切换口留第三方兜底**）
  - 验收：ASR 抽象层；本地+可切第三方；转写返回 quality
- [x] **P6.3** A6 笔记结构化（转写文本→四块 summary/points/decisions/todos；**四块完整性≥80%**；兜底=字段缺失留空待补）
  - 验收：四块结构化完整≥80%；缺失留空待补不强行生成
- [x] **P6.4** 沟通记录挂客户（一次录音拆多项目需求的建模）
  - 验收：录音/笔记挂客户；需求可拆到该客户下不同项目
- [x] **P6.5** A3 提炼素材源接通（沟通记录→A3，端到端语音→需求闭环可验收）
  - 验收：录音→转写→笔记→A3 提炼需求闭环 L3 测试绿

**P6 总验收**：录音→转写→结构化笔记全链路（mock ASR + 真实服务可选），L3 闭环测试绿。

---

## ■ 批次 P7a · 前端业务页·手动流（P4 后并行）

- [x] **P7a.1** 客户管理页（组内客户列表 + 建档触发 A1）
  - 验收：列表+详情+新建/编辑弹窗；越权隐藏；build 绿
- [x] **P7a.2** 项目页 + 需求页（状态机流转 + 组长评审操作）
  - 验收：状态机流转 UI；评审按钮按权限显示；越权隐藏
- [x] **P7a.3** 知识库页（全平台查询 + 开发/组长写入）
  - 验收：全平台查询；写入按角色；无硬编码权限

**P7a 总验收**：手动流（建客户→建项目→录需求→组长评审→交付）前端全通，越权按钮正确隐藏，build 绿。

---

## ■ 批次 P7b · 前端业务页·AI/语音流（P5/P6 后）

- [x] **P7b.1** 录音/笔记页（录音上传 + A6 结构化笔记展示 + 提炼按钮）
  - 验收：录音上传；A6 四块笔记展示；提炼按钮触发 A3
- [x] **P7b.2** 候选箱批量编辑界面（A3 确认）
  - 验收：批量编辑/确认候选需求；确认后才入需求
- [x] **P7b.3** 答疑页（讲师提问 + A4/RAG 回答带引用 + 转人工兜底）
  - 验收：问答带引用；未命中/低置信转人工；build 绿

**P7b 总验收**：AI/语音链路前端全通（A1 建档/候选箱确认/A4 问答/A6 笔记展示），build 绿。

---

## ■ 批次 P8 · 收尾

- [x] **P8.1** 端到端走查（M1→M3 全链，人工录一笔真实数据验证数据流）
- [x] **P8.2** 生产 DoD 自检（功能/可靠/安全/可观测/可恢复五维）
- [x] **P8.3** 文档-实现一致性复核
- [x] **P8.4** git 按批次拆分提交 + 推送

---

## ✅ 每批次通用 DoD

- [ ] L1 本模块单元测试绿
- [ ] L2 关联功能回归绿（若本批改了共享层）
- [ ] 前端 build 通过（涉前端批次）
- [ ] 无 .env/.db 入库，git 干净
- [ ] 落档执行进度
- [ ] **合规检查**：
  - [ ] 组隔离：跨组读被过滤（客户/项目/需求），知识全平台
  - [ ] 审计日志：敏感操作（评审/交付/登录）写 AuditLog
  - [ ] 配置走 `.env`，无硬编码密钥
  - [ ] 越权测试：讲师不能评审、跨组读不到（P4 全矩阵）
